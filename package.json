{
  "name": "openai-image-gateway",
  "version": "0.1.0",
  "description": "OpenAI Images 兼容聚合网关，把 base64 或文件上传转换为上游可访问的图片 URL。",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  }
}
